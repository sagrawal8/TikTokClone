/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'cdn.apartmenttherapy.info', 'lh3.googleusercontent.com'],
  }
}

module.exports = nextConfig
